// Intentionally empty — using i18next default (loose) typing.
// Strict key typing caused interpolation variable type conflicts with number values.
// Keys are still string-safe at runtime via the en/fr locale files.
export {};
