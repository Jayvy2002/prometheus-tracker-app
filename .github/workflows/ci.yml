name: CI

on:
  push:
    branches: [new-JV]
  pull_request:
    branches: [main, new-JV]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
      SUPABASE_PROJECT_REF: phyuijjekxtjvipjtdfv
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run lint
      - run: npm run typecheck
      - run: npm test
      - name: Profile session isolation and upload failures
        run: node scripts/test-profile-session.mjs
      - run: npm run verify:migrations
      - run: npm run verify:edges
      - name: Detect Management API token
        id: token
        run: |
          if [ -n "${SUPABASE_ACCESS_TOKEN}" ]; then
            echo "present=true" >> "$GITHUB_OUTPUT"
          else
            echo "present=false" >> "$GITHUB_OUTPUT"
            echo "::notice title=CLI prod::SUPABASE_ACCESS_TOKEN absent — migration list / dry-run skipped (not a successful proof)."
          fi
      - uses: supabase/setup-cli@v1
        if: steps.token.outputs.present == 'true'
        with:
          version: 2.117.0
      - name: Prod migration list + db push --dry-run
        if: steps.token.outputs.present == 'true'
        run: |
          chmod +x scripts/prod-migration-sync.sh
          bash scripts/prod-migration-sync.sh
      - name: Upload prod CLI proof
        if: always() && steps.token.outputs.present == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: prod-cli-proof
          path: /tmp/prometheus-mig/
          if-no-files-found: ignore
      - run: npm run build

  rls-matrix:
    name: rls-matrix (staging-like)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: supabase/setup-cli@v1
        with:
          version: 2.117.0
      - name: Start local Supabase (staging-like, Postgres 17)
        run: supabase start -x studio,mailpit,realtime,logflare,vector,imgproxy,edge-runtime
      - name: Install psql
        run: sudo apt-get update && sudo apt-get install -y postgresql-client
      - name: Align local vs lock (toutes les versions lock)
        env:
          DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:54322/postgres
        run: node scripts/verify-local-migrations.mjs
      - name: Local migration list (replay)
        run: |
          {
            echo '## supabase migration list --local'
            echo
            echo '```'
            supabase migration list --local || echo 'flag --local indisponible sur cette CLI'
            echo '```'
          } | tee -a "$GITHUB_STEP_SUMMARY"
      - name: Run RLS matrix
        env:
          DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:54322/postgres
          RLS_RESULTS_PATH: /tmp/rls-matrix-results.txt
        run: node scripts/run-rls-matrix.mjs
      - name: Baseline local database advisors
        run: supabase db advisors --local --type all --level info --fail-on none --output-format json > /tmp/advisors-before.json
      - name: Test account capabilities candidate (local database only)
        env:
          DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:54322/postgres
        run: |
          set -euo pipefail
          psql "$DATABASE_URL" -X -v ON_ERROR_STOP=1 -1 -f supabase/changes/account_capabilities.sql
          psql "$DATABASE_URL" -X -v ON_ERROR_STOP=1 -1 -f supabase/changes/account_capabilities.sql
          psql "$DATABASE_URL" -X -v ON_ERROR_STOP=1 -f supabase/tests/account_capabilities.sql
      - name: Test client departure candidate (local database only)
        env:
          DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:54322/postgres
        run: |
          set -euo pipefail
          psql "$DATABASE_URL" -X -v ON_ERROR_STOP=1 -1 -f supabase/changes/client_end_coach_link.sql
          psql "$DATABASE_URL" -X -v ON_ERROR_STOP=1 -1 -f supabase/changes/coaching_relationship_consistency.sql
          psql "$DATABASE_URL" -X -v ON_ERROR_STOP=1 -1 -f supabase/changes/coaching_relationship_consistency.sql
          psql "$DATABASE_URL" -X -v ON_ERROR_STOP=1 -f supabase/tests/client_departure.sql
      - name: Versioned relationship consent (local database only)
        env:
          DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:54322/postgres
        run: |
          set -euo pipefail
          psql "$DATABASE_URL" -X -v ON_ERROR_STOP=1 -1 -f supabase/changes/relationship_consent.sql
          psql "$DATABASE_URL" -X -v ON_ERROR_STOP=1 -1 -f supabase/changes/relationship_consent.sql
          psql "$DATABASE_URL" -X -v ON_ERROR_STOP=1 -f supabase/tests/relationship_consent.sql
      - name: Marketplace publication and request isolation (local only)
        env:
          DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:54322/postgres
        run: |
          set -euo pipefail
          psql "$DATABASE_URL" -X -v ON_ERROR_STOP=1 -1 -f supabase/changes/coach_marketplace.sql
          psql "$DATABASE_URL" -X -v ON_ERROR_STOP=1 -1 -f supabase/changes/coach_marketplace.sql
          psql "$DATABASE_URL" -X -v ON_ERROR_STOP=1 -f supabase/tests/coach_marketplace.sql
      - name: Account entry intention (local only)
        env:
          DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:54322/postgres
        run: |
          set -euo pipefail
          psql "$DATABASE_URL" -X -v ON_ERROR_STOP=1 -1 -f supabase/changes/account_entry_intent.sql
          psql "$DATABASE_URL" -X -v ON_ERROR_STOP=1 -1 -f supabase/changes/account_entry_intent.sql
          psql "$DATABASE_URL" -X -v ON_ERROR_STOP=1 -f supabase/tests/account_entry_intent.sql
      - name: Candidate local database advisors
        run: supabase db advisors --local --type all --level info --fail-on none --output-format json > /tmp/advisors-after.json
      - name: Inventory effective account permissions
        env:
          DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:54322/postgres
        run: node scripts/inventory-account-access.mjs
      - name: Upload advisors and account permission inventory
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: account-access-review
          path: |
            /tmp/advisors-before.json
            /tmp/advisors-after.json
            artifacts/account-access/
          if-no-files-found: warn
      - name: Test questionnaire migrated schema
        env:
          DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:54322/postgres
        run: psql "$DATABASE_URL" -X -v ON_ERROR_STOP=1 -f supabase/tests/coach_questionnaires.sql | tee /tmp/questionnaire-db-results.txt
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Install browser test dependencies
        run: |
          npm ci
          npm install --no-save --package-lock=false playwright@1.56.1
          npx playwright install --with-deps chromium
      - name: Recheck historical RLS with all relationship candidates
        env:
          DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:54322/postgres
        run: node scripts/run-rls-matrix.mjs
      - name: Concurrent coaching transitions (local database only)
        env:
          DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:54322/postgres
        run: node scripts/test-coaching-concurrency.mjs
      - name: Chantier 2 browser journeys (local Supabase only)
        run: node scripts/test-questionnaire-browser.mjs
      - name: Upload questionnaire browser proof
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: questionnaire-browser-proof
          path: artifacts/questionnaire/
          if-no-files-found: warn
      - name: Upload questionnaire database proof
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: questionnaire-db-results
          path: /tmp/questionnaire-db-results.txt
          if-no-files-found: warn
      - name: Publish RLS results
        if: always()
        env:
          DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:54322/postgres
        run: |
          {
            echo '## Matrice RLS (staging-like supabase start PG17)'
            echo
            echo '```'
            psql "$DATABASE_URL" -P pager=off -c "SELECT check_id, passed, detail FROM rls_results ORDER BY check_id" || true
            echo '```'
          } | tee -a "$GITHUB_STEP_SUMMARY"
          mkdir -p artifacts
          cp /tmp/rls-matrix-results.txt artifacts/rls-matrix-results.txt 2>/dev/null || true
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: rls-matrix-results
          path: artifacts/rls-matrix-results.txt
          if-no-files-found: ignore

  deploy-edges:
    name: deploy-edges (CLI)
    needs: verify
    if: github.event_name == 'pull_request' && github.head_ref == 'cursor/audit-securisation-425e'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
      SUPABASE_PROJECT_REF: phyuijjekxtjvipjtdfv
    steps:
      - uses: actions/checkout@v4
      - name: Detect Management API token
        id: token
        run: |
          if [ -n "${SUPABASE_ACCESS_TOKEN}" ]; then
            echo "present=true" >> "$GITHUB_OUTPUT"
          else
            echo "present=false" >> "$GITHUB_OUTPUT"
            echo "::notice title=CLI deploy::SUPABASE_ACCESS_TOKEN absent — functions deploy skipped (not a successful deploy)."
          fi
      - uses: actions/setup-node@v4
        if: steps.token.outputs.present == 'true'
        with:
          node-version: 22
      - uses: supabase/setup-cli@v1
        if: steps.token.outputs.present == 'true'
        with:
          version: 2.117.0
      - name: Deploy coach-fleet-round + coach-agent via CLI
        if: steps.token.outputs.present == 'true'
        run: |
          chmod +x scripts/deploy-audit-edges.sh
          bash scripts/deploy-audit-edges.sh
